# SpendWise Backend

This is the backend for the SpendWise application, built with FastAPI and SQLite.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the server:
   ```bash
   uvicorn main:app --reload
   ```
